# SBC 프로젝트 디자인 시스템 가이드 (v1.0)

본 문서는 SBC 프로젝트의 전역 디자인 원칙, 토큰(Token), 컴포넌트 스타일링 가이드를 정의한 문서입니다. 기획, 디자인, 개발 간의 일관된 커뮤니케이션과 구글 스티치(Google Stitch) 등에서의 원활한 핸드오프를 위해 작성되었습니다.

---

## 1. 디자인 철학 (Design Philosophy)
- **따뜻한 동네 감성**: 차갑고 딱딱한 기업형 UI를 지양하고, 친근하고 귀여우며 따뜻한 무드를 연출합니다.
- **간결하고 심플한 플랫 디자인**: 복잡한 장식, 뚜렷한 선(Border), 불필요한 그림자(Box-shadow)를 최대한 배제하여 미니멀리즘을 추구합니다.
- **둥글고 부드러운 형태**: 모서리 곡률(Border-radius)을 넉넉하게 사용하여 안정감과 귀여운 느낌을 줍니다.

---

## 2. 타이포그래피 (Typography)
- **기본 폰트**: `JayeonSans` (자연산스) - 따뜻하고 친근한 인상을 주는 전역 메인 서체.
- **포인트 폰트**: `YuhanKimberly` (유한킴벌리 푸른숲체) - 캘리그라피 느낌의 강조형 타이틀 서체.
- **기본 타이포그래피 규칙**:
  - `letter-spacing`: `-0.02em` (-2%) (자간을 살짝 좁혀 가독성을 높입니다)
  - `line-height`: `1.6` (160%) (넉넉한 행간으로 답답함을 해소합니다)
  - `font-weight`: `400` (Regular)을 기본 굵기로 사용합니다.

---

## 3. 컬러 시스템 (Color System)
테마 확장을 고려해 글로벌 슬레이트(Slate) 계열 토큰을 뼈대로 삼고, 이를 시맨틱 토큰(Semantic Token)과 매핑하여 사용하는 2계층 구조를 가집니다.

### 3.1. Primitive Tokens (기본 색상)
- `--color-slate-400`: `#AEB4B7`
- `--color-slate-600`: `#626C71`
- `--color-slate-700`: `#3B5462`
- `--color-gray-50`: `#fafafa`
- `--color-background-beige`: `#F0EEED`

### 3.2. Semantic Tokens (텍스트 의미론적 색상)
텍스트의 위계(Hierarchy)에 따라 명도를 나누어 사용합니다.
- **`--color-text-secondary` (`#3B5462`, Slate 700)**: 
  - **용도**: 페이지의 최상단 메인 타이틀(`h1`, `.pageTitle`, `.mainText` 등), 시선이 가장 먼저 닿아야 하는 주요 강조 텍스트, 활성화된 탭 메뉴 버튼.
- **`--color-text-tertiary` (`#626C71`, Slate 600)**: 
  - **용도**: 일반 본문(Body) 텍스트, 탭 메뉴 등 일반 버튼의 폰트 컬러, 서브 타이틀.
- **`--color-text-muted` (`#AEB4B7`, Slate 400)**: 
  - **용도**: 카드 내부의 메타 정보(날짜, 부서명 등), 비활성화된 텍스트 및 버튼.

---

## 4. 레이아웃 및 컴포넌트 원칙 (Layout & Components)

### 4.1. 전역 스타일 규칙
- **테두리 및 그림자 사용 금지**: 플랫한 감성을 유지하기 위해 `border`와 `box-shadow`는 원칙적으로 사용하지 않습니다. (버튼 및 카드 호버(Hover) 시에도 그림자는 넣지 않습니다.)
- **브레드크럼(Breadcrumb)**: 내비게이션 경로는 반드시 `1depth 메뉴명 - 2depth 메뉴명` 형식으로 표기합니다. (예: `나눔터 - 소식·주보`)

### 4.2. 카드(Card) 컴포넌트 가이드
메인 페이지, 성도 사업체(Network), 소식·주보(News), 행사(Events) 등 리스트가 나열되는 전역 공간에 공통으로 사용되는 둥근 카드 규격입니다.

- **그리드 레이아웃**: PC 기준 4단 그리드 (`grid-template-columns: repeat(4, 1fr)`), Gap `16px`.
- **배경색**: `--color-gray-50`
- **디멘전 (PC / Mobile)**:
  - **Padding**: `32px 28px` / `16px 20px` (모바일)
  - **Border-radius**: `48px` / `24px` (모바일 - 매우 둥글고 귀여운 곡률)
  - **Height**: `240px` / `auto` (모바일에서는 내부 콘텐츠 Hug)
  - **Grid Gap**: `16px` / `8px` (모바일)
- **인터랙션 (Hover)**: 마우스 오버 시 그림자 없이 위로 살짝 떠오르는 부드러운 애니메이션만 적용 (`transform: translateY(-2px)`, `transition: transform 0.2s ease`). 자바스크립트 기반 애니메이션 대신 순수 CSS로 성능을 확보합니다.

### 4.3. 탭 및 뱃지 버튼 (Tabs & Buttons)
화면 내 네비게이션을 돕는 가로형/세로형 탭 메뉴 디자인 가이드입니다.

- **기본 버튼 (Inactive)**:
  - 배경색: 투명(`transparent`)
  - 폰트 컬러: `--color-text-secondary-rgb` 투명도 40%
- **선택된 버튼 (Active)**:
  - 배경색: 투명(`transparent`)
  - 폰트 컬러: `--color-text-secondary`
- 탭 메뉴는 기존 알약(Pill) 형태를 탈피하고, 여백(`gap: 24px`)을 넓게 준 텍스트 위주의 간결하고 미니멀한 UI로 개편하여 사용합니다. 모바일에서는 가운데 정렬되며 패딩 및 폰트 사이즈가 기기에 맞게 축소됩니다.

---

## 5. 반응형 및 모바일 대응 (Mobile Responsiveness)
- 모바일 해상도(`max-width: 600px ~ 768px`) 진입 시 카드의 높이, 패딩, 둥글기 곡률을 PC 버전에 비례하게 축소하여 좁은 화면에서도 카드가 둔탁해 보이지 않도록 설계합니다.
- 가변 콘텐츠(편지지 영역, 안내문 등)가 찌그러지지 않도록 부모 컨테이너에 `width: 100%`를 명시하여 안정성을 확보합니다.
